package oncall.domain.onCall;

import java.util.List;

public class OnCalls {

    private final List<OnCall> onCalls;

    public OnCalls(List<OnCall> onCalls) {
        this.onCalls = onCalls;
    }
}
